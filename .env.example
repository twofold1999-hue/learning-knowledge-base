# Local Node proxy configuration. Never commit a real API key.
# The browser does not receive these values. Public deployment needs a backend service.
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-flash
DEEPSEEK_BASE_URL=https://api.deepseek.com
